/** Public path to the résumé PDF, served from the `public/` directory. */
export const RESUME_PDF_URL: string = `${import.meta.env.BASE_URL}vian-garg-resume.pdf`;

/** Filename used when the résumé is downloaded. */
export const RESUME_FILENAME = 'Vian-Garg-Resume.pdf';
