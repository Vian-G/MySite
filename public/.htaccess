RewriteEngine On

# Serve real files and directories normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other paths to the single-page app
RewriteRule ^ index.html [L]
